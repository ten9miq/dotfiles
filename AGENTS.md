# AI 作業ルール

このファイルはリポジトリ全体に適用する。詳細は
[`docs/ai-development-guide.md`](docs/ai-development-guide.md) を参照すること。

## 基本方針

- 変更前に対象の `init.sh` と、その読み込み元・コピー先を確認する。
- 既存の Bash、Zsh、Fish、Linux、WSL、Cygwin の互換性を不用意に壊さない。
- 依頼に必要な最小範囲を変更し、無関係な整形や大規模な書き換えを混ぜない。
- シェルコードでは既存の構成と命名に合わせつつ、新規コードでは変数とパスを原則として引用する。
- 秘密情報、アクセストークン、秘密鍵、端末固有の認証情報を追加しない。個人差のある値はローカル設定へ分離する。
- ユーザー向け・運用向けドキュメントは、明示的な指定がない限り日本語・UTF-8で作成する。
- AI向けの補足ドキュメントは `docs/` 配下に置き、ルートにはAIが検出する `AGENTS.md` だけを置く。

## 安全性

- `setup.sh` は実際の `$HOME` を変更するため、検証目的でそのまま実行しない。
- コピー、上書き、権限変更、シンボリックリンク作成、外部取得を伴う処理は、影響先を確認してから変更する。
- 破壊的な Git 操作や、ユーザーのホームにある既存ファイルの削除を行わない。

## 検証

- 変更したシェルファイルは、利用可能なら `bash -n <file>` と `shellcheck <file>` で確認する。
- Zsh/Fish 固有ファイルは、対応するシェルが利用可能な場合に構文確認する。
- 配置処理の動作確認が必要な場合は、一時ディレクトリを `$HOME` に指定した隔離環境で行う。
- 実行できなかった検証、対象OSでのみ確認できる事項、残るリスクを作業結果に明記する。
